# Advanced Web Vulnerability Scanner - Dependencies
# Install with: pip install -r requirements.txt

# Core HTTP library for web requests
requests>=2.28.0

# HTML/XML parsing library
beautifulsoup4>=4.11.0

# User agent randomization
random-user-agent>=1.0.1

# Additional parsing for XML (for sitemap parsing)
lxml>=4.9.0

# URL parsing utilities (included in standard library but listed for clarity)
# urllib3 is a dependency of requests
